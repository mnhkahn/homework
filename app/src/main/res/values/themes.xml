<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Theme.HomeworkBuddy" parent="android:style/Theme.Material.Light.NoActionBar">
        <item name="android:fontFamily">sans</item>
        <item name="android:colorAccent">@android:color/holo_blue_light</item>
        <!-- Keep Android's own Home/overview bar clearly visible outside Lock Task. -->
        <item name="android:navigationBarColor">@android:color/white</item>
        <item name="android:windowLightNavigationBar">true</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>
    <style name="Theme.HomeworkBuddy.RemoteCapture" parent="Theme.HomeworkBuddy">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:backgroundDimEnabled">false</item>
    </style>
</resources>
